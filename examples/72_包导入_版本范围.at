引用.包导入("网络请求", "^1.0.0");
引用.包导入("工具", ">=0.1.0");

类 包导入版本范围测试 {
公开:
    声明 运行(){
        打印("=== 包导入: 版本范围 ===");
        设 r = 网络请求.发送("http://range.example.com");
        打印("版本范围调用: " + r);
        设 r2 = 工具.请求("rangeA");
        打印("工具范围调用: " + r2);
        返回值 0;
    };
};