引用.文件("lib/atunity/引擎.at")
引用.文件("lib/atunity/演示场景.at")

类 main {
公开:
    声明 运行(){
        设 游戏 = 创建 引擎(800, 600, "AtOS 1.0");
        游戏.设置背景色(AtOS_桌面);
        游戏.设置自动帧数(600);
        全局自动模式 = 真;
        游戏.启动(创建 AtOS场景);
        返回值 0;
    };
};